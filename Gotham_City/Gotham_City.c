#include <stdio.h>

int main() {
unsigned cr;
scanf("%d",&cr);
if(cr < 5)
printf("I got this!");
else if (cr >4 && cr < 11)
printf("Help me Batman");
else
printf("Good Luck out there!");
    return 0;
}
